# - Config file for the vinecopulib package
# It defines the following variables
#  VINECOPULIB_INCLUDE_DIR - include directories for vinecopulib
#  VINECOPULIB_LIBRARIES    - libraries to link against
 
# Compute paths
get_filename_component(VINECOPULIB_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(VINECOPULIB_INCLUDE_DIR "@CONF_INCLUDE_DIRS@/vinecopulib")
 
# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET vinecopulib AND NOT PROJECT_BINARY_DIR)
  include("${VINECOPULIB_CMAKE_DIR}/vinecopulibTargets.cmake")
endif()
 
# These are IMPORTED targets created by vinecopulibTargets.cmake
set(VINECOPULIB_LIBRARIES vinecopulib)